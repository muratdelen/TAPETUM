name: retinextapetum_camera
description: On-device low-light photo enhancement demo powered by RetinexTapetum.
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.12.0+1
  image: ^4.9.1
  image_gallery_saver_plus: ^5.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
